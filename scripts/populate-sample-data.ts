/**
 * Archived script.
 *
 * This script previously seeded legacy submission/story tables that were removed
 * from the schema in Tier 2 cleanup.
 */

console.warn(
  "[archived] scripts/populate-sample-data.ts is disabled because it depended on removed legacy Prisma models.",
);
