/**
 * Archived script.
 *
 * This script previously targeted legacy submission models that were removed
 * from the schema in Tier 2 cleanup.
 */

console.warn(
  "[archived] scripts/migrate-to-unified-model.ts is disabled because it depended on removed legacy Prisma models.",
);
