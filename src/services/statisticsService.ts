export async function updateCityStatistics(city: string, country: string) {
  // Deprecated after CityStatistics model removal.
  // Public destination aggregates are derived via PublicDestinationReadModel.
  void city;
  void country;
}
